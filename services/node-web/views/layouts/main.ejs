<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- typeof title !== 'undefined' ? title + ' — ' : '' %>Space Dashboard</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Leaflet CSS for maps -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
    
    <style>
        body { padding-top: 1rem; background-color: #f8f9fa; }
        .card { margin-bottom: 1rem; }
        .jwst-slider { position: relative; }
        .jwst-track {
            display: flex;
            gap: 0.75rem;
            overflow: auto;
            scroll-snap-type: x mandatory;
            padding: 0.25rem;
        }
        .jwst-item { flex: 0 0 180px; scroll-snap-align: start; }
        .jwst-item img { width: 100%; height: 180px; object-fit: cover; border-radius: 0.5rem; }
        .jwst-cap { font-size: 0.85rem; margin-top: 0.25rem; }
        .jwst-nav { position: absolute; top: 40%; transform: translateY(-50%); z-index: 2; }
        .jwst-prev { left: -0.25rem; }
        .jwst-next { right: -0.25rem; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">🚀 Space Dashboard</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/dashboard">Dashboard</a>
                <a class="nav-link" href="/osdr">OSDR</a>
            </div>
        </div>
    </nav>

    <main class="container">
        <%- body %>
    </main>

    <footer class="container mt-5 mb-3 text-center text-muted">
        <small>Space Dashboard — Node.js/Express (migrated from PHP/Laravel)</small>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
</body>
</html>
